# CLAUDE.md

Claude Code reads this file, not `AGENTS.md`. This project's full agent
instructions live in `AGENTS.md` (single source of truth) — imported below
so both stay in sync automatically.

@AGENTS.md
